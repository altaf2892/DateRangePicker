<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    tools:context="com.savvi.rangepickersample.SampleActivity">

    <LinearLayout android:orientation="vertical"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|center"
        android:gravity="center">
        <com.savvi.rangepickersample.ScrollDisabledListView
            android:id="@+id/calendar_view"

            android:layout_width="wrap_content"
            android:layout_height="250dp"

            android:clipToPadding="false"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:paddingBottom="16dp"
            android:scrollbarStyle="outsideOverlay"
            android:scrollbarThumbVertical="@android:color/transparent"
            app:tsquare_displayHeader="false"
            app:tsquare_inner_l="@color/transparent"
            app:tsquare_dividerColor="@color/transparent"
            app:tsquare_headerTextColor="@color/colorAccent"
            app:tsquare_titleTextColor="@color/colorAccent" />


    </LinearLayout>

    <Button
        android:id="@+id/get_selected_dates"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Selected Dates" />

</LinearLayout>
